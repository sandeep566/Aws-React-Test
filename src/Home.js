import React from "react";
import Loader from "../src/Amazon_icon.png";

const Home = ()=>{
    return(
        <div>  
           
           <img src={Loader} alt={Loader} />

        </div>
    )
}

export default Home;