import React from "react";
import Loader from "../src/logo.svg"

const Home = ()=>{
    return(
        <div>  
           
           <img src={Loader} alt={Loader} />

        </div>
    )
}

export default Home;